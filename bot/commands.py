from discord.ext import commands

'''async def change_playlist(client, config, fileName):
	if '.txt' in fileName:
		await client.send_message(message.channel, 'You have changed AcaBot\'s playlist to {}' .format(fileName))
		config.autoplaylist = fileName
	elif '.txt' not in fileName:
		await client.send_message(message.channel, 'You have changed AcaBot\'s playlist to {}.txt' .format(fileName))
		config.autoplaylist = fileName + '.txt'
'''